<div class="center row no_team">
  <% if current_user.my_teams.present? %>
    <h5>Seus times</h5>
    <div class="row">
      <ul class="collection col m4 offset-m4 s10 offset-s1">
        <% current_user.my_teams.each do |t| %>
          <li class="collection-item dismissable">
            <div>
              <%= t.slug %>
              <a href="/<%= t.slug %>" class="secondary-content">
                <i class="material-icons">call_made</i>
              </a>
            </div>
          </li>
        <% end %>
      </ul>
    </div>
    <div class="row">
      <a class="btn" href="#add_team_modal">Novo time</a>
    </div>
  <% else %>
    <h5>Você ainda não criou nenhuma equipe</h5>
    <h5>Crie um agora mesmo!</h5>
    <a class="btn" href="#add_team_modal">Novo time</a>
  <% end %>
</div>